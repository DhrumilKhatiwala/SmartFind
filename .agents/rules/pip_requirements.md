# Requirement Update Rule

Whenever installing any Python packages using `pip install <package>`, always update [`requirements.txt`](file:///c:/Projects/QueryForge/requirements.txt) immediately to include the newly installed package(s).
